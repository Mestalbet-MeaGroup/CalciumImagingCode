function [frG,frB]=CalcFR9Well(tGcamp,icGcamp,tBase,icBase,res);
%res is in miliseconds.


for i=1:size(icGcamp,2)
    frB(:,i)=histc(sort(tBase{i}),0:res*12:tBase{i}(end))./res;
end


for i=1:size(icBase,2)
    frB(:,i)=histc(sort(tBase{i}),0:res*12:tBase{i}(end))./res;
end

end